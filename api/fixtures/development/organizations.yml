- model: Organization
  rows:
    - _id: nixopus_org
      id: "{{ uuid }}"
      name: "Nixopus Inc"
      description: "Main organization for Nixopus platform"
      created_at: "{{ now }}"
      updated_at: "{{ now }}"

    - _id: demo_org
      id: "{{ uuid }}"
      name: "Demo Organization"
      description: "Demo organization for testing purposes"
      created_at: "{{ now }}"
      updated_at: "{{ now }}"

    - _id: ci_test_org
      id: "0f2eaa45-9947-42b9-b057-b4a673995f53"
      name: "Test Organization"
      description: "Organization for CI integration tests"
      created_at: "{{ now }}"
      updated_at: "{{ now }}"

