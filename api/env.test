HOST_NAME=localhost
DB_PORT=5433
USERNAME=nixopus
PASSWORD=nixopus
DB_NAME=nixopus_test
PORT=8080
SSL_MODE=disable

# Path where configs are mounted
MOUNT_PATH=/tmp/nixopus

# SSH settings
SSH_HOST=localhost
SSH_PORT=22
SSH_USER=runner

# SSH Authentication
SSH_PRIVATE_KEY=/dev/null
SSH_PASSWORD=test

# Docker daemon socket
DOCKER_HOST=unix:///var/run/docker.sock

# Redis connection
REDIS_URL=redis://localhost:6379

# Caddy API endpoint
CADDY_ENDPOINT=http://localhost:2019

# CORS whitelist
ALLOWED_ORIGIN=http://localhost:3000

# Email configuration (for testing)
EMAIL_HOST=localhost
EMAIL_PORT=587
EMAIL_USERNAME=test@example.com
EMAIL_PASSWORD=test
EMAIL_FROM=test@example.com

# Environment
ENV=development